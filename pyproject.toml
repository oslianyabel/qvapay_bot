[project]
name = "qvapay"
version = "0.1.0"
description = "Telegram bot for the documented QvaPay API endpoints"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.2.2",
    "python-telegram-bot>=22.7",
    "requests>=2.33.1",
]
